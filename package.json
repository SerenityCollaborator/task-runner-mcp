{
  "name": "task-runner-mcp",
  "version": "0.1.0",
  "private": true,
  "description": "MCP server for managing long-running background tasks (spawned processes) with in-memory logs.",
  "license": "MIT",
  "type": "module",
  "main": "dist/index.js",
  "bin": {
    "task-runner-mcp": "dist/index.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "node --enable-source-maps dist/index.js",
    "lint": "eslint .",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.1.0",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "@types/node": "^22.13.0",
    "typescript": "^5.6.3"
  },
  "engines": {
    "node": ">=20"
  }
}
